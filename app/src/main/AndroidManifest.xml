<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="carlosmuvi.bqsample">

    <application
        android:allowBackup="true"
        android:name=".BqSampleApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activities.LoginActivity"
            android:label="@string/title_activity_login"></activity>
        <activity
            android:name=".ui.activities.EbookListActivity"
            android:label="@string/title_activity_ebook_list"></activity>
        <activity
            android:name=".ui.activities.EbookDetailsActivity"
            android:label="@string/title_activity_ebook_details"></activity>

    </application>

</manifest>
